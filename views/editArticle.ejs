<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Hack the blog | Edit</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
  </head>
  <body>
    <%- include("partials/navbar") %>

    <div class="container mt-3 shadow" style="max-width: 600px">
      <h1><strong>Edit article <%= user.id %></strong></h1>
      <form action="/articles/edit/<%= user.id %>" method="POST">
        <div class="mb-3">
          <label for="articleTitle" class="form-label">Title</label>
          <input
            type="text"
            class="form-control"
            id="articleTitle"
            name="articleTitle"
            value="<%= article.title %>"
          />
        </div>
        <div class="mb-3">
          <label for="articleContent" class="form-label">Content</label>
          <textarea
            class="form-control"
            id="articleContent"
            name="articleContent"
            rows="3"
            value="<%= article.content %>"
          ></textarea>
        </div>
        <div class="mb-3">
          <label for="articleImage" class="form-label">Image</label>
          <input
            class="form-control"
            type="file"
            id="articleImage"
            name="articleImage"
            value="<%= article.image %>"
          />
        </div>
        <label for="articleAuthor" class="form-label">Select author</label>
        <select class="form-select mb-3" aria-label="selectAuthor">
          <option selected>Select...</option>
          <option value="<%= user.firstname user.lastname %>">author</option>
        </select>
        <button class="btn btn-primary w-100 mb-3">Edit</button>
      </form>
    </div>

    <%- include("partials/footer") %>
  </body>
</html>
